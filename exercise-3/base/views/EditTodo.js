import React, { useState } from "react"
import { Button } from "../components/Button"
import { TextInput } from "../components/TextInput"

export const EditTodo = ({ oldTitle, onEdit, onCancel }) => {
  // Add logic and return UI
  return null
}
